info:
  name: GET OrderConfirmationDetailsRequest
  type: http
  seq: 2

http:
  method: GET
  url: "{{vc_url}}?RequestName=OrderConfirmationDetailsRequest&BuyersID={{vc_user}}&Password={{vc_pass}}&IsTest=false&OrderConfirmationID=OC-2182"
  params:
    - name: RequestName
      value: OrderConfirmationDetailsRequest
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: IsTest
      value: "false"
      type: query
    - name: OrderConfirmationID
      value: OC-2182
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
