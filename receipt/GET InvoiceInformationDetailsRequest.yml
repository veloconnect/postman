info:
  name: GET InvoiceInformationDetailsRequest
  type: http
  seq: 2

http:
  method: GET
  url: "{{vc_url}}?RequestName=InvoiceInformationDetailsRequest&BuyersID={{vc_user}}&Password={{vc_pass}}&IsTest=false&InvoiceID=R2019010"
  params:
    - name: RequestName
      value: InvoiceInformationDetailsRequest
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: IsTest
      value: "false"
      type: query
    - name: InvoiceID
      value: R2019010
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
