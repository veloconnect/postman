info:
  name: GET GetClassificationSchemeRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{vc_url}}?RequestName=GetClassificationSchemeRequest&BuyersID={{vc_user}}&Password={{vc_pass}}&IsTest=false"
  params:
    - name: RequestName
      value: GetClassificationSchemeRequest
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: IsTest
      value: "false"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
