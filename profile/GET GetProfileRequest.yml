info:
  name: GET GetProfileRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{vc_url}}?RequestName=GetProfileRequest&IsTest=false&BuyersID={{vc_user}}&Password={{vc_pass}}"
  params:
    - name: RequestName
      value: GetProfileRequest
      type: query
    - name: IsTest
      value: "false"
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
