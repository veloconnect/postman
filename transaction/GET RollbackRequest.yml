info:
  name: GET RollbackRequest
  type: http
  seq: 2

http:
  method: GET
  url: "{{vc_url}}?RequestName=RollbackRequest&BuyersID={{vc_user}}&Password={{vc_pass}}&IsTest=false&TransactionID=123"
  params:
    - name: RequestName
      value: RollbackRequest
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: IsTest
      value: "false"
      type: query
    - name: TransactionID
      value: "123"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
