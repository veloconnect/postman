info:
  name: GET FinishOrderRequest
  type: http
  seq: 8

http:
  method: GET
  url: "{{vc_url}}?BuyersID={{vc_user}}&Password={{vc_pass}}&RequestName=FinishOrderRequest&TransactionID={{createOrderTransactionId}}&IsTest=false"
  params:
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: RequestName
      value: FinishOrderRequest
      type: query
    - name: TransactionID
      value: "{{createOrderTransactionId}}"
      type: query
    - name: IsTest
      value: "false"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
