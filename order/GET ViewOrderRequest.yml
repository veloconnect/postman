info:
  name: GET ViewOrderRequest
  type: http
  seq: 9

http:
  method: GET
  url: "{{vc_url}}?RequestName=ViewOrderRequest&BuyersID={{vc_user}}&Password={{vc_pass}}&TransactionID={{createOrderTransactionId}}&IsTest=false"
  params:
    - name: RequestName
      value: ViewOrderRequest
      type: query
    - name: BuyersID
      value: "{{vc_user}}"
      type: query
    - name: Password
      value: "{{vc_pass}}"
      type: query
    - name: TransactionID
      value: "{{createOrderTransactionId}}"
      type: query
    - name: IsTest
      value: "false"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
